# Enable ALB with HTTPS
create_alb = true

# Your domain name (must be registered and managed in Route53)
domain_name = "factoringsquare.xyz"

# Subdomain for API (creates api.example.com)
api_subdomain = "api"
